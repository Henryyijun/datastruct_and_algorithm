#include "include.h"
using namespace std;

int main(int arg, char** argv) {
    int a[num] = {2, 1, 3, 1, 0};
    //bubble_sort(a, num);
    show_array(a, num);
    bubble_sort_op(a, num);
    show_array(a, num);
}